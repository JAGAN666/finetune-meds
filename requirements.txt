# Core training stack. Unsloth is installed separately inside Colab with its
# CUDA-specific extras (see notebooks/02_train_qlora.ipynb). Pinning here
# would fight Colab's preinstalled torch; keep it to the rest.

transformers>=4.46.0,<4.50.0
trl>=0.12.0,<0.14.0
peft>=0.13.0,<0.16.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
datasets>=2.20.0,<3.0.0  # MACCROBAT ships a loading script; datasets 3.x removed script support

# Schema + evaluation
pydantic>=2.9.0
jsonschema>=4.23.0
pandas>=2.2.0
pyarrow>=17.0.0

# Constrained decoding
outlines>=0.1.0
lm-format-enforcer>=0.10.0  # fallback if outlines misbehaves with the quantized model

# Synthetic data generation
anthropic>=0.39.0

# NLP helpers (sentence splitting for MACCROBAT prep)
nltk>=3.9.0
